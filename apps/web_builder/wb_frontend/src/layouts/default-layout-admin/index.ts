import { withClasses } from '../../react_utils';
import { DefaultLayoutAdmin as DL } from './default-layout-admin';
import classes from './default-layout-admin.module.scss';

export const DefaultLayoutAdmin = withClasses(classes, DL);
